# Document measured Swift/Rust parity

## Outcome
Update root README with verified native Swift effect count and actual Rust comparison evidence. Separate registered implementation, measured deterministic full-frame cases, CLI terminal/platform gaps and visual recordings. Retain upstream Rust/Python claims as distinct scope. Refresh four-track app/speed/control documentation and link TDD evidence.

## Workflow
Documentation authorized; English. ODD recovery document, existing feature branch codex/comparison-playback-speed. RDD off, no remote/push/PR/merge. User requires TDD always: no behavior changes here; use existing Rust oracle regression suites as factual evidence, do not manufacture RED for passive prose. Source changes still RED/GREEN mandatory. Concurrent speed writer owns runtime/TDD guide, README writer owns only README and this document; no branch switches or source edits.

## Tasks
- [x] R01: Verify both registries and fresh deterministic full-run Rust/Swift comparison using existing suite; record exact command/host/reference and samples. Investigate known limitations only proportionately. No global parity inference.
- [x] R02: Update README with concise measured evidence/status and actual37-effect/four-track features; links/commands verified. Readback and one conventional docs commit, no unrelated source changes.

## Acceptance and checks
State effect count only from actual registry. Claims must identify sample/default/non-default scope, seed/input/canvas/clock and Rust source revision; list unsupported/untested Windows/Linux and CLI lifecycle/layout gaps. Existing 148 videos represent capture integrity not global parity. Use full sequence equality, not only counts/pixels. Run existing full parity suite once with cost forecast, report unavailable/failing honestly. Structural readback and git diff --check for documentation. No remote oracle fetching.

## Progress and rollback
README still says three tracks and focused three-effect CLI proof despite newer suites. Reference baseline0f24d88408c8b815761c2c07da7dc9b411f63016; current effect Rust source unchanged by UI commits, verify. Revert README and this task evidence only. Full CLI portability work remains pending.

## Verified outcome (2026-09-17)
- Actual registry arrays identical, 37/37 names: src/effects/mod.rs name match and Sources/ttfx-swift/Effects/TTFXEffects.swift names. Every name has Swift factory dispatch.
- `git diff 0f24d88408c8b815761c2c07da7dc9b411f63016 -- src Cargo.toml Cargo.lock`: empty; source reference retained. Repository at evidence run11e587ba781f14ea42ad929f0467ab3a6a732ea6; host macOS26.6.2 arm64 Swift6.3.3.
- `swift test --filter CompleteEffectParityTests`: exit0,111 default cases and6 timed cases passed (117total,2functions),69.700seconds. Initial sandbox attempt failed before suite execution because compiler cache path unwritable; approved same-command rerun used existing caches. No download, no source fix, no fabricated RED.
- Cases compare every ANSI frame and count until completion, guard10000ticks; all37 defaults input/seed/canvas: TTFX/Rust+Swift/Visual comparison42/24x8, Native Swift/ A B C/Parity!7/16x6, Parity2026/SWIFT+RUST/  Two spaces123/18x7 at25fps; timed matrix/thunderstorm60,17,0fps seed23 Clock test/Rust Swift16x6. No non-default corpus certification.
- Independent byte comparison of37 existing artifacts/video-comparison/*/rust.frames vs swift-cli.frames:37exact,0missing/different. Sample only; manifest seed42,24x8,25fps,text TTFX\nRust + Swift\nVisual comparison,virtual clocks.148video integrity does not establish semantic parity.
- README refreshed four-track layout, actual Swift CLI files, independent Cmd1/2pane controls,0.5–3xvideo/timeline playback, capture-date footer and TDD guide link. Scope separated from Rust/Python claims; CLI specT01–08 remain pending, Windows/Linux unverified.
- Structural checks: links and referenced commands/files exist; `git diff --check` clean. Runtime harness N/A for passive prose; existing oracle regression and dump comparisons supply factual evidence. Rollback README+this recovery document only.
- Work-unit commit: this conventional docs commit (parent records exact identity after creation); no push/merge/remote/native review, RDDoff.

## Next step
Parent readback and full Engram mirror of this document with commit identity. Continue cross-platform CLI taskT01 only with explicit implementation scope and observed RED/GREEN.
